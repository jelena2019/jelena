export class Planet {
  id: number;
  name: string;
  diameter: number;

  constructor(id: number, name: string, diameter: number) {
    this.id = id;
    this.name = name;
    this.diameter = diameter;
  }
}
