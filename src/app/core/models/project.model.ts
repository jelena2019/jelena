export class Project {
  id: number;
  name: string;
  membersCount: number;
  members: [];

  constructor(id: number, name: string, membersCount: number, members: []) {
    this.id = id;
    this.name = name;
    this.membersCount = this.membersCount;
    this.members = this.members;
  }
}
